services:
  db:
    image: postgres:15
    container_name: expense-db
    environment:
      POSTGRES_USER: user_teste
      POSTGRES_PASSWORD: pass_teste
      POSTGRES_DB: expense_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
